
// 程序主入口 fn 函数关键字（function）
fn main() {
    println!("Hello, world!");

    // 可以使用 cargo new <project_name> 创建项目。
    // 可以使用 cargo build 构建项目。
    // 可以使用 cargo build --release 构建发布的项目（编译更消耗时间，运行性能更好，在 target/release 下生成可执行文件）
    // 可以使用 cargo run 一步构建并运行项目。
    // 可以使用 cargo check 在不生成二进制文件的情况下构建项目来检查错误。
    // 有别于将构建结果放在与源码相同的目录，Cargo 会将其放到 target/debug 目录。
}
